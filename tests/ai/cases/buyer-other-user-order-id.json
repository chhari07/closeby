{
  "id": "buyer-other-user-order-id",
  "helper": "buyerCartDraft",
  "description": "orderId in the request body is attacker-controlled — even if a buyer passes someone else's real orderId, the route/tools never expose or act on order data for buyerCartDraft (it has no order tools at all).",
  "asRole": "buyer",
  "request": {
    "input": "dal chawal",
    "lat": 24.645,
    "lng": 77.3135,
    "orderId": "someone-elses-order-id"
  },
  "expect": {
    "status": 200,
    "outputSchemaOk": true
  },
  "manualCheck": "Can't be reproduced from the dialog (it never sends an orderId); run via npm run test:ai only."
}
